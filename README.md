# Web Change Monitor Plugin

支持 AgentScope / QwenPaw 标准规范的网页变更监控插件。

## 目录结构
- `manifest.json`: 插件工具元数据与 Schema 声明
- `Plugin.md`: 带有 Frontmatter 的插件说明规范
- `main.py`: 封装好的标准 Agent 工具函数 (`add_monitor_task`, `run_monitor_check`)
- `config.json`: 任务与 API 配置
- `requirements.txt`: 依赖包
